# 复制为 .env(放在本 skill 目录)。运营端**只需填这一项**:
# 中间层 multi-channel-api 地址(create / edit / pull / A+ / 传图 全走它)
AMAZON_MCA_URL=http://<中间层那台机器的IP>:8000

# 以下都是可选,有默认值,一般不用动:
# AMAZON_STORE=main                                  # 默认店铺
# AMAZON_ALLOWED_STORES=main,qifengz,serenorch,bfpeaky  # 店铺白名单
# AMAZON_BRAND=Inkelligent
# AMAZON_MARKETPLACE_ID=ATVPDKIKX0DER

# 注意:COS / SP-API 等所有密钥都在【中间层那台机器】的 .env,运营端一概不碰。
